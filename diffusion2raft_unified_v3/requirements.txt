# Core flow model
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0

# Unified Qwen feature extraction and the retained legacy guide generator.
# If QwenImageEditPlusPipeline is unavailable in the installed release, use:
# pip install git+https://github.com/huggingface/diffusers
diffusers>=0.39
transformers>=4.51
accelerate>=1.0
safetensors>=0.4
